(function(){
'use strict';
function N(s){return(s||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toUpperCase()}
const API={'lotofacil':'lotofacil','mega-sena':'megasena','quina':'quina','dia-de-sorte':'diadesorte','lotomania':'lotomania','super-sete':'supersete','timemania':'timemania','mais-milionaria':'maismilionaria','dupla-sena':'duplasena'};
const LABEL={pares:'PARES',impares:'IMPARES',primos:'PRIMOS',fibonacci:'FIBONACCI',moldura:'MOLDURA',centro:'CENTRO',mult3:'MULT. DE 3',soma:'SOMA'};
function prime(n){if(n<2)return false;for(let i=2;i*i<=n;i++)if(n%i===0)return false;return true}
function fib(max){let s=new Set(),a=1,b=1;while(a<=max){s.add(a);[a,b]=[b,a+b]}return s}
function dims(slug){return slug==='lotofacil'?[5,5]:slug==='mega-sena'?[6,10]:slug==='quina'?[8,10]:slug==='lotomania'?[10,10]:slug==='dia-de-sorte'?[7,5]:null}
function stats(nums,slug){let fs=fib(Math.max(...nums)),d=dims(slug),m=0,c=0;if(d){let[cols,rows]=d;nums.forEach(n=>{let z=n-1,r=Math.floor(z/cols),x=z%cols;if(r===0||x===0||r===rows-1||x===cols-1)m++;else c++})}return{pares:nums.filter(n=>n%2===0).length,impares:nums.filter(n=>n%2).length,primos:nums.filter(prime).length,fibonacci:nums.filter(n=>fs.has(n)).length,moldura:d?m:null,centro:d?c:null,mult3:nums.filter(n=>n%3===0).length,soma:nums.reduce((a,b)=>a+b,0)}}
function top3(a){let c={};a.filter(v=>v!=null).forEach(v=>c[v]=(c[v]||0)+1);return Object.keys(c).map(Number).sort((x,y)=>c[y]-c[x]||x-y).slice(0,3)}
async function draw(api,n){let r=await fetch('https://servicebus2.caixa.gov.br/portaldeloterias/api/'+api+(n?'/'+n:''),{cache:'no-store',headers:{Accept:'application/json'}});if(!r.ok)throw Error();let j=await r.json(),a=j.listaDezenas||j.dezenasSorteadasOrdemSorteio||[];return{n:+j.numero||n,nums:a.map(Number).filter(Number.isFinite)}}
function buttons(root){let o={};root.querySelectorAll('button').forEach(b=>{let q=N(b.textContent).trim();Object.keys(LABEL).forEach(k=>{if(q===LABEL[k]||(k==='mult3'&&q==='MULT DE 3'))o[k]=b});if(q==='FIXAS')o.fixas=b});return o}
function latest(root){let t=root.innerText||'',m=t.match(/N[º°]?\s*(\d{2,6})/i)||t.match(/CONCURSO\s*(\d{2,6})/i);return m?+m[1]:null}
function markOptions(root,key,vals){setTimeout(()=>{let title='ESCOLHA AS QUANTIDADES DE '+LABEL[key],els=[...root.querySelectorAll('div,section,fieldset')].filter(x=>N(x.textContent).includes(title));let panel=els.sort((a,b)=>a.querySelectorAll('*').length-b.querySelectorAll('*').length)[0];if(!panel)return;panel.querySelectorAll('button').forEach(b=>{let v=parseInt((b.textContent||'').trim(),10);b.classList.toggle('thor-auto-selected',vals.includes(v))})},60)}
function bind(root,b,key){if(!b||b.dataset.thorAuto)return;b.dataset.thorAuto='1';b.addEventListener('click',()=>{let vals=(b.dataset.thorVals||'').split(',').map(Number).filter(Number.isFinite);if(vals.length)markOptions(root,key,vals)})}
function markFixas(root,nums){let b=buttons(root).fixas;if(!b)return;b.dataset.thorFixas=nums.join(',');if(!b.dataset.thorFixBound){b.dataset.thorFixBound='1';b.addEventListener('click',()=>setTimeout(()=>{let set=new Set((b.dataset.thorFixas||'').split(',').map(Number));let panels=[...root.querySelectorAll('div,section,fieldset')].filter(x=>N(x.textContent).includes('ESCOLHA AS DEZENAS FIXAS'));let p=panels.sort((a,c)=>a.querySelectorAll('*').length-c.querySelectorAll('*').length)[0];if(p)p.querySelectorAll('button').forEach(x=>{let n=parseInt((x.textContent||'').trim(),10);x.classList.toggle('thor-auto-selected',set.has(n))})},60))}}
async function run(root){let input=root.querySelector('.thor-auto-qtd'),btn=root.querySelector('.thor-auto-run');if(!input)return;let qtd=Math.max(1,Math.min(100,+input.value||10)),slug=root.dataset.thorLottery||'lotofacil',api=API[slug];if(!api)return;btn.disabled=true;btn.textContent='...';try{let n=latest(root);if(!n)n=(await draw(api)).n;let rows=[],freq={};for(let i=0;i<qtd;i++){let d=await draw(api,n-i);if(!d.nums.length)continue;rows.push(stats(d.nums,slug));d.nums.forEach(x=>freq[x]=(freq[x]||0)+1)}let bs=buttons(root);Object.keys(LABEL).forEach(k=>{let vals=top3(rows.map(r=>r[k]));if(bs[k]){bs[k].dataset.thorVals=vals.join(',');bind(root,bs[k],k)}});let fixed=Object.keys(freq).map(Number).sort((a,b)=>freq[b]-freq[a]||a-b).slice(0,3);markFixas(root,fixed)}finally{btn.disabled=false;btn.textContent='CALCULAR'}}
function mount(root){if(root.querySelector('.thor-auto-calc'))return;let heading=[...root.querySelectorAll('h1,h2,h3,div,strong')].filter(x=>N(x.textContent).trim()==='CONFIGURACOES AVANCADAS').sort((a,b)=>a.querySelectorAll('*').length-b.querySelectorAll('*').length)[0];if(!heading)return;let box=document.createElement('div');box.className='thor-auto-calc';box.innerHTML='<input class="thor-auto-qtd" type="number" min="1" max="100" value="10" aria-label="Concursos para analisar"><button class="thor-auto-run" type="button">CALCULAR</button>';heading.style.position='relative';heading.appendChild(box);box.querySelector('.thor-auto-run').onclick=()=>run(root)}
function scan(){document.querySelectorAll('.thor-fechamento-luxo').forEach(mount)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(scan,250));else setTimeout(scan,250);document.addEventListener('click',()=>setTimeout(scan,80),true);new MutationObserver(()=>{clearTimeout(window.__thorAutoT);window.__thorAutoT=setTimeout(scan,150)}).observe(document.documentElement,{childList:true,subtree:true});
})();